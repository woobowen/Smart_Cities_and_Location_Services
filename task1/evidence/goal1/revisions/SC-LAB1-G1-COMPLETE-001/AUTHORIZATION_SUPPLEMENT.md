# 当前任务补充决定：执行阶段自主推进，取消重复技术审批

来源：本轮用户直接消息，原文归档。覆盖初始 Prompt 中相冲突的 D1/D2 审批安排；不改写历史。

本消息覆盖 SC-LAB1-G1-COMPLETE-001 中要求再次逐项确认
D1/D2，以及普通技术选择必须返回用户的相关安排。
其余教师任务、真实性、共同审核标准、原始数据保护、
先单项后组合、Goal 阶段范围和 GitHub 二重验收要求不变。

不要重新初始化任务，不要生成下一份提示词。
接续当前状态实际执行。

一、D2 现在明确批准

采用你提出的教学参考删除调度：

在分段后的输入片段上一次计算教师双侧方向差谓词，
完整标记后同时删除；首尾、缺少必要方向或零位移导致
无法计算的窗口保留并标记；不迭代删除至收敛；
删除后重算相关特征。

这是本次明确批准的 Goal 1 参考实现，
不是“教师已经明确所有实现细节”，也不是“已经证明最优”。

归档本次批准，不再询问是否批准、是否再次确认。
实现和审核发现普通工程问题，内部修复。
Goal 2 再按既定计划评价必要的替代策略，
不能为了取得更好分数在 Goal 1 静默换方法。

二、D1 的事实状态与允许工作分开

当前没有新增的文件级坐标来源说明可提供。
不要将本消息当作 WGS84、GCJ02 或其他坐标基准的证明。

由你负责核查已经可访问的真实来源。
已有材料已查无结果且没有新线索时，不反复扫描，
也不再弹窗问我是否突然有这份说明。

本次新增授权：
若无法确认原始 datum，但已有证据支持坐标顺序、
角度单位和数据结构，则允许在明确、可追踪的分析假设下，
完成固定 pilot 的开发性处理、几何计算、工具验证和闭环。

具体要求：

1. source_crs 的事实状态继续保持 UNVERIFIED。
2. 分别记录原始数据事实、分析模型假设和实际采用的工作坐标。
3. 自主选取与任务尺度相符、具有技术依据的局部计算方案；执行前固定其定义、单位、参数与核验办法。不能根据清洗得分挑一个“看起来最好的 CRS”。
4. 原始经纬度和时间不改写。工作坐标只作为派生数据；不擅自做加偏转换，不把角度直接当米，不通过随意指定 EPSG 标签伪装坐标事实已经查明。
5. 检查计算实现、局部投影／近似误差及其对阈值判断的影响。计算方案之间一致，不等于原始 datum 已获证明；敏感性检查也不能消除未知来源造成的全部不确定性。
6. 这类产物明确标为“真实数据上的条件化分析”，不是构造数据，也不是已确认真实地理精度的正式结果。无依据的绝对定位、道路匹配和真实地面误差结论不得产生。
7. 仍按可信原始输入、独立合同和实际产物审核，不能因为增加了分析假设就放宽点归属、原值保护、DP 数值保证、账本和版本一致性检查。
8. 若连坐标顺序／单位或合理的分析假设都无法支持，或其不确定性使目标结论无法判断，只阻断受影响结论，完成其余工作并最终集中说明，不编造答案。

这项授权允许条件化开发继续，不等于把 D1 改成事实已确认。
工程完成、条件化实验有效、真实地理语义确认三项分别登记。
不得把未完成的语义确认项强行改成 PASS，
也不得仅因该项未知就放弃全部可完成的真实处理与审核。

三、后续技术事项采用授权内自主执行

当前 Goal 范围内的实现细节、工具组织、代码修复、
测试补充、复算、必要图表和可逆工程选择，由你自行完成，
不再向我逐项请求批准。

存在多个可行选择时：
先按教师要求、已批准方法和现有证据排除不适用项；
其余按已固定的正确性、质量、保护条件与复杂度原则决定。
没有必要的研究分歧不做大规模试验；
需要候选效果研究的内容仍放在对应 Goal，
不能以“自主”为理由提前开展 Goal 2/3。

任何“最优”只能指明确候选、指标、数据和预算范围内的结果。
不得使用候选自己生成的真值、修改指标、删除不利样本、
放宽容差或隐藏负结果来证明更优。

四、不要再用确认弹窗代替正常执行

撤销旧 Prompt 中“启动后必须向用户提问 D1/D2”的任务指令。
对本消息已解决的批准问题，不再次调用用户问答工具；
不将等待逐项确认作为任务推进前提。

对数据事实缺口采用上述处理政策。
普通失败进入 A诊断 → B执行/修复 → C独立审核
→ 回归 → 恢复父任务，不返回一份“请下一次修复”的报告。

外部权限、额外付费、破坏性操作和平台安全规则不在此授权内；
不要通过关闭安全机制换取表面无人值守。
真正无法继续时保存检查点，集中说明必要阻碍，
不宣称已经完成，也不假装后台仍在运行。

五、同步与交付

小范围同步当前任务合同，以及 active AGENTS/研究协议中
与本次执行授权直接冲突的条款，保留原始历史记录。
不要扩张或重写全部治理文件。

完成已有两处审核缺口修复，继续固定 pilot、真实工具执行、
必要角色闭环、内部审核、回归、Notebook 和证据整理。

内部验收按实际能力和明确条件逐项给出结果；
满足当前授权下的交付条件后 commit/push，
返回固定 SHA、完整结果和仍未证明的事实，
供 GPT 读取真实远程二重验收。

不把条件化分析冒称无条件完成，
不自行进入 Goal 2/3，
不再生成一份需要我转发的新执行提示词。
现在继续执行。
