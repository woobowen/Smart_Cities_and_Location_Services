# A 角色交接：G1 剩余任务与研究门槛

本交接派生自本轮 [批准 Prompt](../handoff/SC-LAB1-G1-COMPLETE-001_CODEX_PROMPT.md)，不替换 [requirements.json](../../../requirements.json)，也不是内部验收结论。完整可执行字段、依赖及 GC / 原 G1 映射见 [TaskPlan.json](TaskPlan.json)。主线程消费本交接后继续原 Goal，不能将 A 子任务结束当成 G1 完成。

## 1. 已核实的事实

- 当前基础 HEAD 为 `7e6cd4082c2cdad2452fa5a1006e99dcfb050d17`；检查时工作区已有治理文档改动和用户 handoff ZIP。A 仅写本 `a_scope/` 目录。
- 直接核对的六个教师/starter 文件与旧 extraction index 的 SHA256 全一致；PPT13 正文、备注及 PPT20 的三个已保存摘录与原 XML 逐字一致。不重复渲染 43 页。
- 原始 JSON hash 仍为 `c59be4c079d2ffd8ae0127c8a361c77ba277252abb2c7202efb9ee4e8e6084d3`。本次只读核查固定记录 `0、1、2、246、256、306、352` 合计 783 点，没有执行空间清洗。
- Notebook Cell1 原文确认 `65/5/30/400/35/5`；Cell10 为 split→denoise→simplify，短片段过滤属于分段步骤中的独立处理。PPT24 的 95m 是另一教学例子。它们不能被写成最优参数。
- PPT20 定义 outgoing direction 的双侧方向差谓词，但没有给出同时/顺序/迭代删除调度。原 `utils.get_direction` 复制末方向的做法也不能代替获批端点合同。
- 当前合同和 COMPLETE §7 明确保留 D1/D2 未决。在本轮 handoff、active 合同、来源注册及相关原文的定点核查中，没有找到新的文件绑定坐标事实或用户 D2 批准。该结论不覆盖未提供的会话或外部数据方答复。

完整来源、hash、原文和每记录点数在 [source_audit.json](source_audit.json)。可重跑：

```bash
.venv/bin/python task1/evidence/goal1/revisions/SC-LAB1-G1-COMPLETE-001/a_scope/verify_sources.py
```

## 2. 不依赖 D1/D2 的必需工程

| 顺序/任务 | 实际动作 | 关闭条件 |
|---|---|---|
| A-T02 | 在原 requirements 中修正旧范围/状态；小范围治理同步；更新当前入口 | 原 R01–R16 保留；11 文件分发字节/hash一致；历史保留且不再覆盖本轮授权 |
| A-T03 | C2-F01：runtime 审核目标与覆盖绑定 | 时间边界、等号、索引及完整重复事件反例被拒绝，正确产物通过；旧 profile PASS 不能关闭新目标 |
| A-T04 | C2-F02：可信原始输入＋候选外合同审核 | clean/final 同改、漏记录/账本、999汇总、偷换参数/父版本/过滤理由拒绝；合法空输入/全过滤正确处理 |
| A-T05 | Goal 队列＋真实 B-Repair 消费＋C 复验＋父任务恢复 | 不以 `phase==5` 结束 Goal；实际 issue→repair→test→review→rebuild→resume 有记录；新产物审核缺项不能关闭 |
| A-T06 | 本 Goal 累计资源与恢复 | 主会话/子角色/turn/工具分开计；旧4次/6次和冻结保留；unknown不伪造；未知完成状态不自动重发 |
| A-T07 | 固定有效 CODE_SHA；最终语法/工作测试和相关回归 | 全部 `task1/tests` 通过，影响范围明确；源码变化使受影响旧结果失效并重建 |
| A-T08 | 从 raw 重建当前 profile/time/duplicate 诊断并独立审核 | 固定七条全覆盖、每个目标hash与checked/unchecked匹配；完整空间链缺项仍明确 BLOCKED |
| A-T09 | 两工作 Notebook 新内核执行 | 默认 RECOMPUTE 从原始输入算当前工具结果、零新模型调用；真实修复/恢复记录可复核 |
| A-T10 | 必要原生图与架构同步 | 实际 B-Repair/C/父任务返回路径可见；`.drawio`及SVG/PDF一致、渲染可读；不重复生成未变图 |
| A-T14/15 | 独立 C 总审查、唯一当前 REVIEW_PACKET、安全发布 | 内部缺陷继续修；正常全部通过才标工程就绪，外部阻断版本只能按实际范围审核并标 BLOCKED |

上述任务不能因为 D1/D2 未获答复而停止。实际角色协作可由本轮真实 A/B/C 和主线程承接；不要求重跑六次旧诊断角色展示。确定性数值复算不需要额外 LLM。

## 3. 需要研究输入的分支

| 门槛 | 需要什么 | 不足以解除的依据 |
|---|---|---|
| D1_DATUM_FACT | 与当前 JSON 对应的原始坐标基准、加偏/转换历史及来源说明 | PPT普遍性说明、函数名、历史局部平面实现、底图观感 |
| D1_DISTANCE_APPROVAL | 基准事实之后明确批准转换/投影、单位、适用范围与误差合同 | 只知道datum；候选自己提供adapter；改APPROVED字符串 |
| D2_DIRECTION_APPROVAL | 用户明确确认推荐的一次完整标记后同时删除、端点/不可算保留、不迭代、之后重算 | GPT推荐、总体架构合理、继续修复、构造样例approval |

主线程已经集中提出 D1/D2，A 不重复询问。若用户后续只允许近似假设，必须保留“假设”身份及范围，不能标成数据事实；当前 Prompt 还没有该权限。starter `DECISIONS.md` 恰好也有 D1/D2 编号，但它们是历史距离/异常检测决策，不是本次研究批准；不能引入其噪声地板或曲率规则。

门槛齐备后依次完成：

1. **A-T11**：归档真实来源/批准，登记候选外 production contract 和坐标适配器，保留 raw→working 对应，独立测试。普通索引、日志、等价实现细节自行落实。
2. **A-T07**：固定新代码并重跑受影响最终回归，再生成产物；不能在原 run 内热替换源码。
3. **A-T12**：固定七条、783点真实分段→短片段过滤→获批方向去噪→有限线段DP，完整账本和候选外独立审核。保留0删除这一合法结果；未运行阶段不得填0。
4. **A-T13**：实际 C 结果驱动后续局部核验/工程修复，B 实际执行后 C 审核对应新产物。若无错误，可针对实际最大DP误差片段或出现的不可算方向窗口补核验；不制造问题或强求提升。
5. 更新 Notebook、必要真实前后图及总验收；这些必须使用同一版代码/配置/结果。

若只得到 D1，可以实现/核验获批坐标适配与空间原语，完整链仍待 D2；若只得到 D2，可以完成数学方法注册和构造审核，真实方向处理仍待 D1。任何部分批准都不能解锁其余未批准部分。

## 4. 必须在原表修正的旧记录

1. **R03 阶段分配**：当前 G2 字段含“明确删除调度后实际基线”；最新 Prompt 要求获批后在 G1 执行真实基线，G2 保留参数与效果研究。
2. **snapshot / R08 / R12**：仍写旧 run03 的“只有A成功，B失败，C未调用，反馈未完成”。REPAIR 的实际诊断角色链已存在，应分别保留旧失败、继承的新诊断证据、本轮真实修复角色记录；不能仍用旧冻结阻止新授权，也不能将诊断链冒充真实处理链。
3. **missing_provided_research_files**：三项仍写 missing，但 `task1/docs/research/` 已有对应文件和 hash，不要重复索取或重搜文献。
4. **R05 / G1-A08**：描述性指标合同和有效 profile 审核可继承其范围；C2 已证明的目标覆盖/基线候选自证部分必须重开，不能整项照抄历史 PASS。
5. **R14 / 架构 / Notebook**：现有五阶段图和结束文字需按真实控制器修改；Notebook01 的固定 `assert baseline['status']=='BLOCKED'` 不能长期堵死获批运行，Notebook02 不能只复算历史链。
6. **当前 README / CONTRACTS 修复附录 / sources registry**：旧6-call耗尽和“下一步仅等待”是历史批次规则。当前授权入口应指向 COMPLETE；原 ledger/live_stop 不删。

这些都是最新已批准要求和现有文件之间的实质不一致，不是风格建议。由主线程在各文件职责内更新；A 没有更改 active 表、合同或代码。

## 5. GC 与原 G1 的关闭语义

`TaskPlan.json` 包含 GC01–GC16 每项的任务和关闭标准，并完整映射 G1-A01–A18。关键限制如下：

- G1-A04 历史 PASS 的范围是“未知语义正确门控”，不能推导 D1 已确认。
- G1-A07 的 DP 数学 PASS 可以继承；真实米制链和受影响评价器仍需当前独立审核。
- G1-A08 因 C2-F01/F02 重开受影响范围。G1-A12 历史诊断反馈确实发生，但 C05 当时没有覆盖完整请求目标；不能倒写历史。
- GC10 / G1-A11 必须有真实七条完整处理；GC11 必须有真实处理反馈。构造、旧诊断、拒绝正确或 BLOCKED 门控均不能使它们变为 PASS/N/A。
- GC15 只有“所有独立必需工程已完成，剩余确实全部依赖外部研究输入”时才允许外部阻断退出。完成两个 patch 不满足此条件。
- GC16 必须实际内部复验、push、核对远程。旧发布记录不能证明本轮发布。最终 GPT 二重审核保持 PENDING。

G2 收益阈值、三组参数扫描、顺序/四模式对照、论文采用数、全量清洗、两份正式报告和教师 ZIP/邮件均不属于本轮额外阻碍。G1 也不要求所有描述性指标变好。

## 6. A 的实际验证与边界

本角色运行了 `ls -la`、只读 Git/来源/代码检查和 `verify_sources.py`：6文件hash一致、3摘录逐字一致、raw hash一致、7条783点。未运行外部 Provider、未新建代理、未安装包、未修改原始数据/合同/治理/工作代码或旧证据。

本交接是研究诊断和任务规划结果。工程修复、最终测试、Notebook和图产物、内部 C 总审查及发布由主线程继续；A 不在此宣布 G1 完成。
