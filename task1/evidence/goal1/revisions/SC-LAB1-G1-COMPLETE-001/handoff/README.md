# Goal 1 新 Codex 对话交接包

## 唯一执行入口

`SC-LAB1-G1-COMPLETE-001_CODEX_PROMPT.md`（TXT 为逐字节相同副本）。

这是实际工程执行任务，不是独立审计任务，也不是要求生成另一份提示词。
复制主提示词全文到新 Codex 对话即可；也可以将本包解压到本机，并让 Codex 完整读取主提示词，按其要求立即实施。

## 本包与当前工程的关系

本轮重新读取远程 main，核对起点为 `7e6cd4082c2cdad2452fa5a1006e99dcfb050d17`。Codex 开始执行时仍须重新检查实际远程和本地改动；不 reset 到此提交。

保留现有代码与有效成果，修复 C2-F01、C2-F02，接通同一 Goal 内的执行—修复—复验—恢复。不能只复验然后再次生成 NEXT_CODEX_PROMPT。

## reference_only

此目录为过去的真实审阅报告和探针。它们只提供缺陷依据与回归输入，不是最新执行指令，不允许其“审阅后返回”等旧角色安排覆盖主提示词。

- `GPT_SECOND_REVIEW_ROUND2.md`：历史二重验收报告。
- `probes/verify_round2_findings.py`：历史可执行探针，请按脚本实际参数对当前仓库运行；接口变更时保留反例语义并说明适配。
- `probes/previous_round_results.json`、`probes/probes_current.json`：不同历史检查时点的真实结果，不是本轮新运行。

没有附带旧源码快照、旧执行提示词、教师大数据和环境包，避免误覆盖和重复建立工程。主提示词本身已经写明缺陷、验收与恢复要求，缺参考附件时仍足够启动修复。

## 不能通过提示词伪造的前提

D1 的文件对应坐标事实、距离合同与 D2 的单次同时删除方法确认，目前仍需真实证据/明确批准。主提示词要求尽早集中处理这些事项，同时继续不依赖它们的工程。缺失前提不能改字符串解锁，也不能用构造数据代替真实 baseline 后声称 Goal 1 完整通过。

## 交付与复核

完成整个 Goal 的内部验收后，实际 commit/push 并回读远程，返回完整结果供网页 GPT 二重验收。若只剩真实外部阻碍，可发布准确标识的受阻检查点，但不能宣称完成。保存环境和原始材料，不上传凭据。

`MANIFEST.json` 仅用于核对本交接包字节，不是实验验收结果。
