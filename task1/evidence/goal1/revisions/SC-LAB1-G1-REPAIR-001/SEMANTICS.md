# D1 / D2 定点核查

D1：UNKNOWN / 未批准。原始 JSON SHA256 c59be4c079d2ffd8ae0127c8a361c77ba277252abb2c7202efb9ee4e8e6084d3。
教师 PPT 第13页正文与备注只给一般性 WGS84 / GCJ02 描述，没有绑定这份 JSON。新增 Stage01 历史审计同样记录 UNVERIFIED；文献与 Source Audit 的距离算法说明不能识别这份数据的 CRS。未发现老师新补充。具体原文和文件 hash 见 source_excerpts.json；研究文件原字节导入来源见 handoff_provenance.json。只核查相关语义段落，没有新做广泛论文检索。

给数据提供方的聚焦问题（未代发）：这份 traj_dict.json 原始坐标是什么基准、是否加偏，是否有与文件对应的数据说明？确认后仍需批准距离/投影、单位、误差适用范围与实际参数；不以数值外观认定 WGS84。

D2：GPT 推荐但用户未批准。候选为输入片段一次计算双侧方向谓词、同时删除；首尾与无法计算必要方向的点保留并标记；不反复迭代，下游重新计算特征。当前 Prompt §4 明确不构成真实方法批准。

工程上注册了该数学候选，并在明确平面构造输入上执行完整链及独立核验。生产 REAL_CONTRACTS 为空；配置状态字符串或构造 approval 对象均不能注册真实数据合同。没有实现未经批准的默认投影。真实 baseline 输出具体 D1 / D2 / 合同与适配器缺失阻断，不以 no-op 伪装处理。

真实 pilot 仍固定 0、1、2、246、256、306、352。Goal2/3、候选筛选、参数搜索、组合、训练、全量正式清洗与正式报告定稿均未执行。
