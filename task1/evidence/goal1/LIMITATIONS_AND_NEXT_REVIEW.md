# 核心限制与下一次审核

总状态 **PARTIAL_BLOCKED**；不是完整Goal1完成。GPT_SECOND_REVIEW=PENDING；实验一 Deliverable=IMPLEMENTING，Understanding由用户确认，Submission=NOT_READY。本轮未执行Goal2/3。

| ID | 问题与证据 | 影响 | 建议和不确定性 |
|---|---|---|---|
| U01_CRS_DISTANCE | [材料发现](materials/findings.md)及[合同](../../docs/goal1/CONTRACTS.md)：PPT13的WGS84/GCJ02均为一般说明，未绑定JSON；Notebook Mercator与旧Agent局部椭球平面不同 | 真实米制距离、速度、空间切分、长度过滤、方向去噪、DP与前后轨迹图阻断 | 优先获得教师/数据来源明确说明；随后由GPT/用户确认距离口径。若只能采用假设，必须显式批准并说明敏感性，不能自行填WGS84 |
| U02_DIRECTION_EXECUTION | [两个构造反例](materials/direction_ambiguity_fixtures.json)证明同时/在线/迭代标记会删不同点 | 去噪函数仅输出候选与不可计算窗口，正式删除及完整链阻断 | 保留PPT双侧循环角差谓词；请GPT/用户明确删除调度、最后方向、零位移窗口策略。建议先审核“一次候选标记、保留无法判定点”是否符合教学意图；这只是候选解释，不是已批准实现 |
| U03_LIVE_TRANSPORT | [run03](runs/g1-live-20260926-03/manifest.json)：A成功，B路由失败，C和反馈未发生；[ledger](runs/budget_live.json)冻结 | G1-A09/A12阻断；不能把工程审查子代理或Notebook复算替代真实角色 | 先解决当前账户/工作区路由，再审核每进程重试配置的实际效果与新预算；保持相同授权访问方式。当前不能证明服务端请求总数或计费次数 |
| U04_RUNTIME_GUARDRAIL | [独立审查](tests/INDEPENDENT_CONTROL_REVIEW.md)记录恢复伪成功、缓存、状态、事件、进程组及诊断审核缺口 | 已修复并离线验证；原live内部重试护栏失败仍保留，A13=FAIL | 把修复后的live权限与预算作为下一轮先决验证。离线108pass不冲销自然运行失败 |
| U05_QUALITY_CONTRACT | [配置](../../config/goal1.json)主要收益阈值/保护项退化额度/选择数据仍null | 不能自动QUALITY_ACCEPTED或宣称候选优于基线 | 先定义要优化的任务收益，再冻结数值门槛和统计口径；无真实标签不称准确率、误删率或真值恢复 |

## Goal2 前草案（未授权执行）

第一步应补齐并验收上述Goal1缺口，再开展参数/顺序/模式实验。当前不采用任何论文增强算法，不用文献库替代教学基线；缺少用户提到的三份历史研究文件已记录，未伪造原文。

- 参数范围仅供审核：以 STARTER_REFERENCE 为中心，时间阈值候选15/30/60秒、空间阈值200/400/800意图米、最小长度32.5/65/130意图米、点数3/5/10、方向20/35/50度、DP2.5/5/10意图米。米制项须U01解决，方向项须U02解决；这些是粗诊断范围，不是由实测得出的最佳范围，不进行笛卡尔积扫描。建议先每次只动一项、其余固定，以便识别原因。
- 共同硬条件：原始值不改、全输入去向守恒、不跨记录/断段连线、索引有序、独立区间误差不越DP阈值、不可计算指标不补最优值。saving只评价相同简化前参考；删除量本身不算收益。
- 主要收益门槛与可容许退化暂不赋数值。没有真值时优先比较描述性压缩/保真/审核支持，同时逐例复核异常；不得以未批准综合分数替代共同尺度。
- 划分：这7条全属开发案例，另有旧starter暴露记录209。全体数据的结构分布已查看；未来划分必须记录这种暴露，不冻结未经批准的最终留出比例。本轮没有最终确认集上的方法选择。
- 若恢复LIVE，建议先申请最多5个串行角色调用、禁止自动重提、最多8个数值工具请求的诊断闭环预算；首个调用同时检验实际transport限制。任何新预算需覆盖所有底层重试，不能按新run_id清零。此处仅为下一次审批建议，不解锁当前配置。
- 单项候选优先是经批准的教学基线本身、一个明确的去噪调度候选或一个针对实际失败的诊断改动；不得先组合。论文候选数为0，暂不实现SHoTClean等算法。Goal3才研究已独立验证策略的兼容性与组合，之后再冻结/全量/正式报告。

## 报告与范围

Experiment Report：仅阶段素材，未写最终研究结论。Process Report：仅保存本任务可见角色/工具记录和工程失败；没有生成聊天截图、伪造Human–AI争论、Evidence Master annotation或Evidence Lock。构造挑战统一ENGINEERING_TEST，不包装为自然Agent错误。教师提交ZIP、邮件、实验二/三、训练、路网匹配、通用平台均未执行。
